# @symbiot/ui

Shared React UI: shadcn/ui components + `ThemeProvider`, sidebar, top bar, selection toolbar.

Wraps shadcn primitives with symbiot tokens from `@symbiot/tailwind-config`.

## Status

Placeholder. Phased rollout:

- **Phase 2** — `ThemeProvider` + minimal top bar.
- **Phase 3** — full sidebar (annotation list, global comments, versions), selection toolbar, mode toggle.
- **Phase 7** — System/light/dark with OS default, AA-contrast annotation tokens, theme toggle.

## Scripts

- `bun run typecheck`
- `bun run lint`
- `bun run test`
