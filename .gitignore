# dependencies
node_modules

# build output
dist
.output

# track the hook plugin's bundled artifacts — Claude Code reads them from
# the cloned repo via /plugin install, so they must ship with the source.
!apps/hook/dist/
!apps/hook/dist/**

# turborepo
.turbo

# cache
.cache
*.tsbuildinfo
.eslintcache

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs
*.log

# coverage
coverage
*.lcov

# OS
.DS_Store

# IDE
.idea
.vscode
.cursor

# Claude Code runtime state
.claude/*.lock

# Playwright + playwright-bdd
.features-generated
test-results
playwright-report
